# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration (Local Only)
MONGO_URI=mongodb://127.0.0.1:27017/civicconnect

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_change_this_in_production
JWT_EXPIRE=30d

# Security
BCRYPT_ROUNDS=10
